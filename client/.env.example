# Copy to .env.production (or set as a build-time env var on your static host)
# and point it at your deployed server before running `npm run build -w client`.
VITE_SERVER_URL=https://api.hidingsnake.io
